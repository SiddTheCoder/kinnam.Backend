//Cookie setting
export const cookieOptions = {
      httpOnly: true,
      secure: true,
      sameSite: "Strict"
}

//email regex
export const emailRegex = /^[\w.-]+@[a-zA-Z\d.-]+\.[a-zA-Z]{2,}$/;

